# Showcase assets

README images live in `screenshots/` as committed PNGs so GitHub renders them without any build step.

To replace images, export new PNGs into `screenshots/` (same filenames) or update the paths in the root [README.md](../README.md).
